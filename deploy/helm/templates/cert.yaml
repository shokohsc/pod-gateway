apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.webhook.certName | default "vpn-egress-webhook-cert" }}
  labels:
    app: vpn-egress-webhook
spec:
  secretName: {{ .Values.webhook.certName | default "vpn-egress-webhook-cert" }}
  dnsNames:
    - vpn-egress-webhook.{{ .Release.Namespace }}.svc
  issuerRef:
    name: {{ .Values.webhook.issuer.name }}
    kind: ClusterIssuer
